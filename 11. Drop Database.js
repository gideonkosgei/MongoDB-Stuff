/*

you can drop a database by switching to that database and running either 
   the db.dropDatabase() method or 
   the dropDatabase command.

The db.dropDatabase() Method
---------------------------
	use test
   db.runCommand( { dropDatabase: 1 } )
   

*/